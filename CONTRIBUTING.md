
Contributing
Guidelines
Never commit secrets

Run git status before commit

Use feat/fix/docs/refactor commit types

Adding Features
Create branch: git checkout -b feature/name

Make changes

Commit: git commit -m "feat(scope): description"

Push and create PR
